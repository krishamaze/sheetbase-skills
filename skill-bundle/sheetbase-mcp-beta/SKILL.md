---
name: sheetbase-mcp-beta
description: Use when an external agent or user needs to operate the beta Sheetbase MCP server, including recon-first workflows, metadata-aware reads and writes, safe column tools, safe row deletion, SQL transforms, or Sheetbase beta error recovery.
---

# Sheetbase MCP Beta

IF task asks how to connect, authenticate, find spreadsheets, choose an initial workflow, or recover from auth errors
  THEN READ `references/using-sheetbase.md`.

IF task asks which Sheetbase beta tool to call, what parameters are required, or how beta differs from production
  THEN READ `references/tool-contracts.md`.

IF task may write, append, clear, format, transform, delete, restore, move structure, or call raw batchUpdate
  THEN READ `references/safety.md`.

IF task involves an unfamiliar workbook, broad transformation, destructive edit, formula maintenance, note metadata, or row deletion
  THEN READ `references/beta-workflows.md`.
